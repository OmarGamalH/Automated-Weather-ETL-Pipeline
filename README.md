# Automated-Weather-ETL-Pipeline
Automated ETL pipeline that extracts weather data from a public REST API, transforms and validates it using Python, and loads historical records into PostgreSQL. Apache Airflow orchestrates the workflow, while Docker provides a consistent containerized environment for automated data processing.
